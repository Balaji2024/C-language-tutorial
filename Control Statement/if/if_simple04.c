#include<stdio.h>
void main()
{
	int age; 
	printf("Enter age ");
	scanf("%d",&age);
	if(age=20)           // it is always true beacause 20 is true integer value and it is always true 
	                    //  20 is always assign to the age and is always true
	printf("A");
	printf("B");
	
}
