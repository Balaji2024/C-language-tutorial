#include<stdio.h>
void main()
{
	int age;
	printf("Enter age ");
	scanf("%d",&age);
	if(age==20)         // it is true only when age is 20..... other than all valuse are false
	printf("A");
	printf("B");
	
}
