#include<stdio.h>
#include<string.h>
void main()
{
	int len,i;
	char s1[20];
	
	printf("Enter s1 string\n");
	gets(s1);
	len=strlen(s1);
	for(i=0;i<len;i++)
	{
		if(s1[i]==' '&&s1[i-1])
	}
	printf("Word are present %d times ",);	
} 
